# Contract Evolution Policy

- Prefer backward-compatible changes
- Breaking changes require new versions and a compatibility window
- APIs should use versioned paths or schema versions
- Events should use versioned type names (v1, v2)

Use casino-b/specs/EVOLUTION.md as reference.
