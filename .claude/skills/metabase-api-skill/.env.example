# Metabase Connection Configuration

# Metabase instance URL (required)
METABASE_URL=http://localhost:3000

# Authentication Method 1: API Key (recommended)
METABASE_API_KEY=your-api-key-here

# Authentication Method 2: Username/Password (alternative)
# METABASE_USERNAME=user@example.com
# METABASE_PASSWORD=your-password

# Optional: Default database ID for operations
# METABASE_DEFAULT_DATABASE_ID=1

# Optional: Default collection ID for new cards
# METABASE_DEFAULT_COLLECTION_ID=5

# Optional: Session token (if you already have one)
# METABASE_SESSION_TOKEN=your-session-token

# Notes:
# - Use either API_KEY or USERNAME/PASSWORD, not both
# - API keys are recommended for automation/CI
# - Session tokens expire after 14 days by default
# - Copy this file to .env and fill in your values
